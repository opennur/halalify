# Gecko Web Extensions

Bundled GeckoView WebExtensions used by Shellify engine sessions. Each extension directory contains
its manifest and runtime assets and is installed through `GeckoEngineManager`. The content
protection extension also bundles the Human runtime, face/body model weights, and regional overlay
script for local smart blocking.
